# TimeGAN
Implementation of the TimeGAN architecture in PyTorch, a hybrid generative adversarial model that combines supervised learning, autoencoding, and adversarial training to generate realistic synthetic sequential data while preserving temporal dynamics and statistical properties.
